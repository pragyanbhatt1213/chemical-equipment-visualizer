[build]
builder = "RAILPACK"

[deploy]
startCommand = "cd backend && python manage.py migrate && gunicorn backend.wsgi --workers 1 --timeout 120"
healthcheckPath = "/api/health/"
healthcheckTimeout = 300
restartPolicyType = "on_failure"
